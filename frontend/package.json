{
    "name": "turbocapture",
    "type": "module",
    "private": true,
    "scripts": {
        "check": "bun i && tsc --noEmit",
        "build": "bun i && vite build",
        "serve": "bun i && vite",
        "test": "bun i && bun test",
        "lint": "bun i && biome check"
    },
    "devDependencies": {
        "@biomejs/biome": "^2.5.11",
        "@types/bun": "^1.4.0",
        "@types/node": "^26.4.0",
        "typescript": "^7.0.2",
        "vite": "^8.2.2"
    }
}
